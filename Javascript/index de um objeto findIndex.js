const lista = [
	{ name: 'Geomembrana', percentual: 0 },
	{ name: 'Geotextil', percentual: 0 },
	{ name: 'Geogrelha', percentual: 0 },
	{ name: 'Gcl', percentual: 0 },
	{ name: 'Geocelula', percentual: 0 },
	{ name: 'Geocomposto', percentual: 0 },
];

const index = lista.findIndex(item => item.name === 'Geomembrana');

// saida = 0;

// arrays

const listaFrutas = ['uva', 'laranja','maca'];

const indexFruta = listaFrutas.findIndex(item => item === 'laranja');

// saida 1
