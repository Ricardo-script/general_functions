import { Widget } from "./components/Widget";

export default function Pattern() {
    return (
        <main className="h-screen bg-zinc-5 dark:bg-black flex items-center justify-center text-zinc-50 ">
            <Widget />
        </main>
    );
}
