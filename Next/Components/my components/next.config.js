//Primeiro, ative os componentes estilizados no next.config.js.

/** @type {import('next').NextConfig} */
const nextConfig = {
    compiler: {
        styledComponents: true,
    }
}

module.exports = nextConfig
